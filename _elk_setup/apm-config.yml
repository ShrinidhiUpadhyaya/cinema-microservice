

apiVersion: v1
kind: ConfigMap
metadata:
  name: apm-server-config
data:
  apm-server.yml: |
    apm-server:
      host: "0.0.0.0:8200"
      rum:
        enabled: true
    output:
      elasticsearch:
        hosts: ["elasticsearch:9200"]
        username: "elastic"
        password: "changeme"
