apiVersion: v1
kind: ConfigMap
metadata:
  name: filebeat-config-notification
data:
  filebeat.yml: |
    filebeat.inputs:
    - type: log
      enabled: true
      paths:
        - /var/log/notification-service/*.log
      scan_frequency: 10s
      close_inactive: 5m
      ignore_older: 24h
      clean_inactive: 72h

    output.logstash:
      hosts: ["logstash-service:5044"]
      #-------------------------------- Enable compression --------------------------------
      #Values between 1 to 9. 0 will disable compression
      #compression_level: 3
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
spec:
  selector:
    app: notification-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3002
  type: LoadBalancer
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: notification-service:v1.0.1
        imagePullPolicy: Never
        ports:
        - containerPort: 3002
        env:
        - name: PORT
          value: "3002"
        volumeMounts:
        - name: log-path-notification
          mountPath: /var/log/notification-service
      - name: filebeat
        image: docker.elastic.co/beats/filebeat:8.13.4
        args: ["-c", "/etc/filebeat.yml", "-e"]
        volumeMounts:
        - name: filebeat-config-volume-notification
          mountPath: /etc/filebeat.yml
          subPath: filebeat.yml
        - name: log-path-notification
          mountPath: /var/log/notification-service
          readOnly: true
      volumes:
      - name: filebeat-config-volume-notification
        configMap:
          name: filebeat-config-notification
      - name: log-path-notification
        hostPath:
          path: /var/log/notification-service

